apiVersion: 1

datasources:
  - name: Revenant
    uid: revenant-sqlite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      path: /data/revenant.db
